{
  "name": "aimachine",
  "private": true,
  "description": "Monorepo for @aimachine packages",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter \"./packages/**\" run build",
    "check": "pnpm --filter \"./packages/**\" run check",
    "test": "pnpm -F @aimachine/browser test && pnpm -F @aimachine/todos test && pnpm -F @aimachine/filesystem test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "puppeteer",
      "esbuild"
    ]
  },
  "packageManager": "pnpm@11.1.0"
}

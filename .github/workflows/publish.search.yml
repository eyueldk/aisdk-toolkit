# Publishes @eyueldk/aisdk-toolkit-search when the package changes on main.
#
# npm Trusted Publishing (OIDC): configure this workflow filename on npm for the package:
#   https://docs.npmjs.com/trusted-publishers/

name: Publish @eyueldk/aisdk-toolkit-search

on:
  push:
    branches: [main]
    paths:
      - "packages/search/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: ./.github/workflows/reusable.publish.yml
    with:
      package: "@eyueldk/aisdk-toolkit-search"
